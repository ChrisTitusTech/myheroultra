---
import RoleBadge from './RoleBadge.astro';
import CharacterArtwork from './CharacterArtwork.astro';
import type { CharacterRecord } from '../data/characters';

interface Props {
  character: CharacterRecord;
}
const { character } = Astro.props;
---
<article class="card">
  <a class="card__artwork-link" href={`/characters/${character.slug}`} tabindex="-1" aria-hidden="true">
    <CharacterArtwork character={character} />
  </a>
  <div class="card__topline">
    <div class="content-badges">
      <RoleBadge role={character.role} />
      <span class="season-badge">S{character.releaseSeason}</span>
    </div>
    <span class="difficulty">{character.isAlternative ? character.battleStyle : `Difficulty ${character.stats.difficulty}/10`}</span>
  </div>
  <h2><a href={`/characters/${character.slug}`}>{character.name}</a></h2>
  <p>{character.summary}</p>
  <dl class="card__stats">
    <div><dt>HP</dt><dd>{character.stats.hp}</dd></div>
    <div><dt>Mobility</dt><dd>{character.stats.mobility}/10</dd></div>
    <div><dt>Difficulty</dt><dd>{character.stats.difficulty}/10</dd></div>
  </dl>
</article>
