---
import type { CharacterRole } from '../data/characters';

interface Props {
  role: CharacterRole;
}
const { role } = Astro.props;
const roleClass = role.toLowerCase();
---
<span class:list={['role-badge', `role-badge--${roleClass}`]}>{role}</span>
